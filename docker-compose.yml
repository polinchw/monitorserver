version: '2'
services:

  monitor-server:
      build: .
      environment:
        SPRING_PROFILES_ACTIVE: docker-compose
      stdin_open: true
      tty: true
      ports:
      - 8080:8080/tcp
      labels:
        io.rancher.container.pull_image: always